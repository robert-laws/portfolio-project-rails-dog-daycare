<%= provide(:title, 'New Reservation') %>

<h1 class="title">New Reservation Form</h1>

<%= render partial: 'shared/form_errors', object: @reservation, as: 'obj' %>

<%= render partial: 'form', locals: { submit_text: "Create New Reservation" } %>