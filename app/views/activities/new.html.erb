<%= provide(:title, 'New Activity') %>

<h1 class="title">New Activity Form</h1>

<%= render partial: 'shared/form_errors', object: @activity, as: 'obj' %>

<%= render partial: 'form', locals: { submit_text: "Create New Activity" } %>