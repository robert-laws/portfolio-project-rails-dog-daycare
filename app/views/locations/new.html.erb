<%= provide(:title, 'New Location') %>

<h1 class="title">New Locaiton Form</h1>

<%= render partial: 'shared/form_errors', object: @location, as: 'obj' %>

<%= render partial: 'form', locals: { submit_text: "Create New Location" } %>